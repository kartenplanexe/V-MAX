# Administrative access only through docker exec as the postgres OS user.
local   all      postgres                          peer
local   all      all                               reject
# Local TLS probe and Serverless Containers in the attached Yandex VPC.
hostssl maxbot   maxbot    127.0.0.1/32             scram-sha-256
hostssl maxbot   maxbot    198.19.0.0/16            scram-sha-256
host    all      all       0.0.0.0/0               reject
host    all      all       ::/0                    reject
